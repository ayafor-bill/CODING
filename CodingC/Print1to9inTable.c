#include <stdio.h>

int main(){
        int A[20], num, i;
        
        for(i = 0; i <= 100; i++){
                A[i];
        }
        
        printf("Enter number to find: ");
        scanf("%d", &num);
        
        for(int j = 0; j <= 100; j++){
                if(A[i] == A[j]){
                        printf("This number is found in %dth position of the array", i);
                }
        }
        
        
    return 0;
}